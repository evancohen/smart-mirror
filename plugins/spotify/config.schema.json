{
    "schema": {
        "spotify": {
            "type": "object",
            "title": "Spotify",
            "properties": {
                "access_token": {
                    "type": "string",
                    "title": "Spotify token returned from oauth"
                },
                "refresh_token": {
                    "type": "string",
                    "title": "Spotify refresh token returned from oauth"
                }
            }
        }
    },
    "form":[
        {
          "title": "Spotify Settings",
          "type":"fieldset",
          "expandable":true,
          "order":0,
          "items":[
            {
              "type":"help",
              "helpvalue": "<strong><em>(used to authorize the api for use with this application)</em> found in plugins/spotify/oauth/</strong>"
            },
            "spotify.access_token",
            "spotify.refresh_token"
          ]
        }
    ]
}