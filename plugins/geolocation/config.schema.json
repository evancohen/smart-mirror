{
    "schema": {
        "geoPosition": {
            "type": "object",
            "title": "{{title}}",
            "properties": {
                "key":{
                    "type": "string",
                    "title": "{{apikey}}"
                },
                "latitude": {
                    "type": "string",
                    "title": "{{latitude}}"
                },
                "longitude": {
                    "type": "string",
                    "title": "{{longitude}}"
                }
            }
        }
    },
    "form":[
        {
          "title": "{{title}}",
          "type":"fieldset",
          "expandable":true,
          "order":0,
          "items":[
            "geoPosition.key",
            {
              "type":"help",
              "helpvalue": "{{helpvalue}}"
            },
            "geoPosition.latitude",
            "geoPosition.longitude"
          ]
        }
    ]
}