{
    "schema": {
        "lastfm": {
            "type": "object",
            "title": "{{title}}",
            "properties": {
                "key": {
                    "type": "string",
                    "title": "{{api key}}"
                },
                "user": {
                    "type": "string",
                    "title": "{{username}}"
                },
                "refreshInterval": {
                    "type": "number",
                    "title": "{{refresh cycle}}",
                    "default": "0.6"
                }
            }
        }
    },
    "form": [
        {
            "type": "fieldset",
            "key": "lastfm",
            "expandable": true,
            "order":0
        }
    ]
}